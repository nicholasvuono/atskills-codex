# Versioning policy

Semver, strictly:

- **patch** — bug fixes, docs, internal refactors; no behavior change
- **minor** — new features, new flags, anything additive
- **major** — removed or changed behavior; requires a migration note in the
  release notes

Every release bumps exactly one of the three. No release reuses a version.
